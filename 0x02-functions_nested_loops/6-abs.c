#include "main.h"

/**
 * _abs - compute the absolute value of an interger.
 *
 * @n: the interger to be compute
 *
 * Return: The absolute value of the interger
 *
 **/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
